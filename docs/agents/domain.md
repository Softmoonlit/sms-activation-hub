# 领域文档

本仓库采用单上下文布局。

探索和修改前应读取：

- 根目录 `CONTEXT.md`
- 与当前工作相关的 `docs/adr/`
- 需要完整业务状态时读取 `docs/domain-model.md`

输出中的领域术语必须遵循 `CONTEXT.md`，不得漂移到其中明确要求避免的同义词。
如工作内容与 ADR 冲突，必须明确指出，不能静默覆盖。
缺失的领域术语应交由 `domain-modeling` 澄清。
