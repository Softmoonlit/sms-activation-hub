# PostgreSQL 容器专用配置模板
# 用法：
#   cp .env.postgres.prod.example .env.postgres.prod
#   chmod 600 .env.postgres.prod
#   # 填入与 .env.prod 中 POSTGRES_PASSWORD 相同的密码

POSTGRES_PASSWORD=replace-with-strong-postgres-password
