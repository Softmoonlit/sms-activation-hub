# 短信激活授权

本上下文描述管理员向指定接收者授予一次 OpenAI 短信激活流程的能力，以及该授权与 HeroSMS 号码激活之间的关系。

## 参与者与授权

**管理员**：
唯一能够创建和撤销授权、配置默认候选地区并处理异常的人。
_避免使用_：后台用户、操作员、商户

**接收者**：
通过私发授权链接获取号码并查看验证码的人，不拥有网站账号。
_避免使用_：用户、客户、会员

**接收者标识**：
管理员填写的内部标识，用于区分授权记录；它不会用于接收者认证，也不会向接收者展示。
_避免使用_：用户名、账号、登录名

**激活授权**：
管理员授予某一接收者在固定期限内获取最多三个 OpenAI 号码的资格。
_避免使用_：订单、购买、链接

**授权链接**：
承载激活授权的私密 bearer URL；首次获取号码时绑定接收者浏览器，授权绝对期限届满后不可再访问。
_避免使用_：登录链接、邀请链接、号码链接

**领取**：
接收者首次明确点击获取号码，使授权链接绑定当前浏览器并开始第一次号码获取。
_避免使用_：登录、注册、激活链接

**授权撤销**：
管理员终止接收者对激活授权的访问；它不表示 HeroSMS 已取消当前号码。
_避免使用_：取消号码、退款

**授权到期**：
激活授权自创建起满 24 小时后终止接收者访问；它不截断仍在进行的 HeroSMS 激活。
_避免使用_：号码超时、激活到期

**授权状态**：
激活授权自身的生命周期状态，只描述接收者是否仍有访问和获取资格，不描述 HeroSMS 号码的结果。
_避免使用_：激活状态、订单状态

## 地区与获取

**候选位置**：
激活授权内三个有序获取额度位置之一；每个位置绑定一个候选地区并且最多产生一次成功的号码获取，不同位置可以绑定同一地区。
_避免使用_：线路、节点、号码池

**候选地区**：
候选位置绑定的 HeroSMS 国家或地区；同一激活授权的多个候选位置可以使用相同候选地区。
_避免使用_：候选位置、线路、节点、号码池

**号码获取**：
HeroSMS 成功返回激活 ID 和号码并产生一次供应商激活；无库存或未产生激活的失败不构成号码获取。
_避免使用_：领取、验证、换号

**号码获取请求**：
系统为取得一个号码而实际调用 HeroSMS 的动作；它可能成功、明确失败或产生需要对账的不确定结果。
_避免使用_：购买请求、号码获取

**获取额度**：
一次激活授权最多允许的三次号码获取；每次成功获取消耗一个候选位置和一次额度。
_避免使用_：三次换号、重试次数

**换号**：
接收者在允许取消后明确放弃当前号码，并在供应商确认取消后自动获取另一个未使用候选位置所绑定地区的号码。
_避免使用_：重试、重新发送验证码

## 供应商激活

**供应商激活**：
HeroSMS 对单个号码的一次有界生命周期，由唯一激活 ID 标识；一个激活授权可以依次关联最多三个供应商激活。
_避免使用_：订单、授权、链接

**当前激活**：
一个激活授权唯一尚未结束的供应商激活；创建后继激活前，当前激活必须先进入明确终态。
_避免使用_：最新号码、当前订单

**激活状态**：
单次供应商激活的生命周期状态，只描述号码获取、验证码、取消、完成和对账结果，不描述接收者的访问资格。
_避免使用_：授权状态、订单状态

**供应商取消**：
HeroSMS 在尚未收到验证码时确认终止供应商激活并按供应商规则返还资金。
_避免使用_：授权撤销、换号、退款申请

**激活超时**：
供应商激活在其号码有效窗口内未收到验证码而结束。
_避免使用_：授权到期、链接过期

**号码有效窗口**：
单次供应商激活从号码获取开始的 20 分钟使用区间，独立于激活授权的 24 小时绝对期限。
_避免使用_：授权期限、链接有效期、完成窗口

**短信送达**：
HeroSMS 已收到目标服务的短信；它终止当前激活的取消和换号资格，但不表示系统已经取得可展示的验证码。
_避免使用_：OpenAI 验证成功、验证码已识别

**验证码识别**：
系统已从 HeroSMS 取得可直接展示的结构化验证码；它只证明短信验证码可用，不证明 OpenAI 验证成功。
_避免使用_：短信送达、验证成功、账号激活成功

**供应商完成**：
短信送达后，系统要求 HeroSMS 结束供应商激活的状态；完成确认失败不撤销已经发生的短信送达。
_避免使用_：OpenAI 验证成功、用户成功

**供应商对账**：
系统使用激活 ID 和供应商最终状态确认结果不明确的号码获取、取消、完成或退款。
_避免使用_：重试、猜测结果

**退款确认**：
系统已取得足以将某次激活费用计入已确认退款的供应商依据；它不决定取消后能否获取下一个号码。
_避免使用_：供应商取消、预计退款

## 数据与成本

**敏感交付数据**：
当前流程所需的完整号码、验证码和短信正文；流程不再需要时立即删除。
_避免使用_：历史记录、审计数据

**激活费用**：
HeroSMS 为一次成功号码获取返回的实际扣费金额。
_避免使用_：报价、用户付款、订单金额

**净成本**：
一个激活授权的累计激活费用减去已确认供应商退款。
_避免使用_：售价、利润、应收金额
