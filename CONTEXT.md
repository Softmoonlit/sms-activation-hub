# 短信激活授权

本上下文描述管理员发放 OpenAI 短信激活资格，以及接收者使用该资格产生 HeroSMS 供应商激活的过程。

## 参与者与授权

**管理员**：
唯一能够创建和撤销激活授权、配置默认候选地区并处理异常的人。
_避免使用_：后台用户、操作员、商户

**接收者**：
通过私发授权链接获取号码并查看验证码的人，不拥有网站账号。
_避免使用_：用户、客户、会员

**激活授权**：
管理员发放的一份 OpenAI 短信激活资格；待领取时不属于特定接收者，也不包含候选位置，领取后允许依次获取最多三个号码。
_避免使用_：订单、购买、链接

**授权链接**：
承载激活授权的私密 bearer URL；待领取时永久有效，领取后只能由绑定浏览器继续使用。
_避免使用_：登录链接、邀请链接、号码链接

**领取**：
接收者首次明确点击获取号码，使激活授权绑定当前浏览器、取得当时配置的三个候选位置并启动领取期限。
_避免使用_：登录、注册、激活链接、签收

**浏览器绑定**：
领取后将激活授权限制在首次操作的浏览器；绑定凭据丢失后不能重新绑定或恢复。
_避免使用_：登录会话、接收者账号

**领取期限**：
从领取起 24 小时内开始号码获取或换号的资格；期限届满不截断此前已经取得的当前激活及其结果查看期。
_避免使用_：创建期限、号码有效期、绝对授权期限

**结果查看期**：
短信在有效窗口内送达后，接收者查看当前号码和验证码的五分钟只读阶段。
_避免使用_：授权期限、号码有效窗口

**激活授权结束**：
激活授权不再允许接收者访问或获取号码的终局结果；管理员撤销、结果查看期结束、获取额度用尽或领取期限收尾均可导致结束。
_避免使用_：供应商取消、激活完成

**授权撤销**：
管理员立即结束激活授权并清理接收者访问；它不表示 HeroSMS 已经取消当前号码。
_避免使用_：取消号码、退款

**撤销收尾**：
激活授权撤销后，当前尚未结束的供应商激活继续走向明确终局的阶段；接收者访问已经终止，但供应商取消或完成事实仍待确认。
_避免使用_：等待短信、供应商取消、退款申请

## 地区与获取

**候选位置**：
领取时从管理员设置实例化的三个有序获取额度位置之一；每个位置绑定一个候选地区并且最多产生一次成功的号码获取。
_避免使用_：线路、节点、号码池

**候选地区**：
候选位置绑定的 HeroSMS 国家或地区；同一激活授权的多个候选位置可以使用相同候选地区。
_避免使用_：候选位置、线路、节点、号码池

**号码获取**：
HeroSMS 成功返回激活 ID 和号码并产生一次供应商激活；无库存或未产生激活的失败不构成号码获取。
_避免使用_：领取、验证、换号

**号码获取请求**：
系统为取得一个号码而实际调用 HeroSMS 的动作；它可能成功、明确失败或产生需要对账的不确定结果。请求结果是一次历史尝试事实，失败不表示候选位置当前不可用。
_避免使用_：购买请求、号码获取

**获取额度**：
一次激活授权最多允许的三次号码获取；每次成功获取消耗一个候选位置和一次额度。剩余额度表示仍可成功获取号码的数量上限，不表示供应商当前有库存。
_避免使用_：三次换号、重试次数

**换号**：
接收者在允许取消后明确放弃当前号码，并在供应商确认取消后按未使用候选位置的原始顺序获取后继号码。
_过渡说明_：统一号码获取流程票据只收敛生命周期；在候选顺序规则票据完成前，现有运行流程仍按报价排序。
_避免使用_：重试、重新发送验证码

**结束使用**：
接收者在允许取消后明确放弃当前号码，但不获取后继号码；适用于最后一个号码或领取期限已经届满的当前号码。
_避免使用_：退出、完成验证、供应商取消

## 供应商激活

**供应商激活**：
HeroSMS 对单个号码的一次有界生命周期，由唯一激活 ID 标识；一个激活授权可以依次关联最多三个供应商激活。
_避免使用_：订单、授权、链接

**当前激活**：
一个激活授权唯一尚未结束的供应商激活；创建后继激活前，当前激活必须先进入明确终态。
_避免使用_：最新号码、当前订单

**激活状态**：
单次供应商激活的生命周期状态，只描述号码获取、验证码、取消、完成和对账结果，不描述接收者的访问资格。
_避免使用_：授权状态、订单状态

**供应商取消**：
HeroSMS 在尚未收到验证码时确认终止供应商激活并按供应商规则返还资金。
_避免使用_：授权撤销、换号、退款申请

**激活超时**：
供应商激活在其号码有效窗口内未收到短信而结束。
_避免使用_：激活授权结束、链接过期

**号码有效窗口**：
单次供应商激活从规范化的供应商取得时间开始。可信的供应商截止时间优先；截止时间缺失、无效、不晚于取得时间或不通过现有合理性校验时，回退到取得时间加 20 分钟。该窗口独立于领取期限。
_避免使用_：领取期限、结果查看期

**短信送达**：
HeroSMS 已收到目标服务的短信；它终止当前激活的取消和换号资格，但不表示系统已经取得可展示的验证码。
_避免使用_：OpenAI 验证成功、验证码已识别

**验证码识别**：
系统已从 HeroSMS 取得可直接展示的结构化验证码；它只证明短信验证码可用，不证明 OpenAI 验证成功。
_避免使用_：短信送达、验证成功、账号激活成功

**供应商完成**：
短信送达后，系统要求 HeroSMS 结束供应商激活的状态；完成确认失败不撤销已经发生的短信送达。
_避免使用_：OpenAI 验证成功、接收者完成

**供应商对账**：
系统使用激活 ID 和供应商最终状态确认结果不明确的号码获取、取消、完成或退款。
_避免使用_：重试、猜测结果

**退款确认**：
系统已取得足以将某次激活费用计入已确认退款的供应商依据；它不决定取消后能否获取下一个号码。
_避免使用_：供应商取消、预计退款

## 数据与成本

**敏感交付数据**：
当前流程所需的完整号码、验证码和短信正文；流程不再需要时立即删除。
_避免使用_：历史记录、审计数据

**激活费用**：
HeroSMS 为一次成功号码获取返回的实际扣费金额。
_避免使用_：报价、用户付款、订单金额

**净成本**：
一个激活授权的累计激活费用减去已确认供应商退款。
_避免使用_：售价、利润、应收金额

## 号码表示

**国际号码**：
供应商返回的完整 E.164 号码；接收者页面按"国际呼叫代码 + 国内号码"两部分拆分展示。
_避免使用_：完整号码

**国际呼叫代码**：
国际号码最前面的国家/地区代号（如 +1、+44）；显示上与国内号码用不同颜色区分，不随"复制号码"按钮拷贝。
_避免使用_：国家代号、区号

**国内号码**：
去掉国际呼叫代码后的纯数字部分；"复制号码"按钮拷贝的形态。
_避免使用_：号码本体
